# ReadTrace 项目约定

## 项目目标

建设面向公司领导层、策划岗和运营岗的小红书舆情监控系统。

第一版聚焦大家电领域，通过监控品牌相关帖子及评论，识别负面舆情及其核心原因，同时为后续扩展到更多品类和平台保留清晰边界。

## 当前阶段

- 当前处于产品需求讨论与范围确认阶段。
- 未确认产品方案前，不进入编码实现。
- 较大改动先给出方案，获得用户确认后再实施。

## 产品原则

- 第一版优先解决“发现负面舆情、定位原因、回看原始证据”。
- 分析结论必须能够追溯到原帖或原评论。
- 品牌、产品品类、情感倾向、问题类型采用独立字段，不互相替代。
- 分类体系允许后续新增品类、问题类型、品牌和数据来源。
- 自动分类需要保留人工修正能力和原始分析结果。
- 面向非技术岗位的界面优先使用业务语言，避免暴露实现细节。

## 数据与合规

- 采集方案必须单独评估平台规则、账号安全、个人信息保护和数据使用边界。
- 帖子至少取得一条评论后才允许入库；没有评论的帖子只保留本地采集产物，不写入业务数据库。
- 保存原始内容时记录来源链接、采集时间和内容更新时间，保证证据可追溯。
- 密钥、账号、Cookie、Token 和密码不得写入代码、文档、日志或提交记录。

## 文档与归档

- 产品方案、需求拆解和工作日志统一存放在 `docs/`。
- 非平凡任务需要在 `docs/worklogs/` 留存工作日志。
- 重要范围或架构决策在 `docs/decisions/` 单独记录。

## 沟通与输出

- 默认使用中文，结论先行，再解释原因。
- 遇到模糊需求，先给出推荐方案及其依据，再指出需要用户确认的选择。
- 不使用压缩、生硬的电报体表达；优先采用自然、完整的中文句子。

## 禁止事项

- 禁止在没有原始帖子或评论证据的情况下输出确定性舆情结论。
- 禁止把情感倾向、问题类型和产品品类合并成同一个分类字段。
- 禁止只展示关键词频次，却不提供对应原帖和原评论的回溯入口。
- 禁止为了第一版速度，把品牌、品类或问题类型硬编码成不可扩展的固定逻辑。

## Git提交

- 每次代码修改完成并通过必要验证后，立即创建Git提交，不等待用户再次提醒。
- 提交时只纳入当前任务范围内的文件，不混入用户或其他任务的未提交改动。
