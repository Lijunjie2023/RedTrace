# 采集数据持久化阶段工作记录

日期：2026-08-06

## 当前目标

在小红书真实数据链路验证通过后，设计并实现MySQL 8.0采集数据持久化、迁移和幂等写入能力。

## 已确认范围

- 首个迁移版本覆盖采集与配置数据。
- DeepSeek分析、人工修正、原因主题、页面和导出后续单独拆解。
- 数据库信息通过本地环境变量提供，真实连接信息不进入代码、文档、日志或Git提交。
- 编码交给子Agent，本体负责规格、测试、审查和项目沟通。

## 规则读取情况

- 已读取项目`AGENTS.md`、已确认PRD和`code-writer`、`product-breakdown`、`feature-breakdown`技能说明。
- 项目外引用的`rules/no_ai_style.md`、`rules/error_log.md`、`rules/code_rules.md`和`rules/sub_agent_dispatch.md`在当前工作区未找到，本阶段按照项目级约定和可读取的技能流程执行。

## 进度

- 已确认B1实体生命周期和牵连关系，并写入`docs/pm-20260806-collection-persistence-ac.md`。
- 已确认B2空状态、失败处理、引用约束、并发和撤销规则。
- 已确认B3输入来源、输出去向和上下游失败边界。
- 用户要求数据库配置和验证通过后直接进入下一步，B4子功能优先级和B5验收标准已完成。

## 本地数据库配置

- 真实连接信息填写在项目根目录`.env.local`，该文件已由`.gitignore`排除。
- 仓库只保留不含真实值的`.env.example`字段模板。
- 连接字段完整性检查通过，未输出任何配置值。
- 本机MySQL客户端可用，TCP端口连接成功。
- 首次MySQL只读连接测试在读取初始握手包时被服务端或网络策略断开，随后确认TCP端口可达。
- 用户更新RDS白名单后，网络连接和账号认证已经通过。
- 当前MySQL返回错误码1044，说明该账号没有访问`.env.local`所配置目标数据库的权限。需要为该账号授权目标数据库，或改用已经获权的数据库名后重新测试。
- 用户反馈完成授权后再次验证，指定目标数据库连接仍返回1044。
- 不指定数据库时账号登录成功，可见4个数据库；`.env.local`配置的目标数据库不在可见列表中，`SHOW GRANTS`也没有出现该数据库。判断为授权对象、实例、账号或数据库名称不匹配，而不是网络、白名单或密码问题。
- 在RDS账号管理页核对后，当前账号已经对现有项目数据库拥有读写（DDL+DML）权限，不需要修改云端授权。
- 根因是`.env.local`沿用了模板数据库名，与控制台现有数据库名不一致；本地配置已经改为当前账号唯一可见的业务数据库。
- 修正后健康检查通过：MySQL版本8.0.36、目标数据库匹配、SSL已启用。
- 唯一命名的权限探针表成功完成创建、增加索引和删除，说明迁移所需CREATE、ALTER、INDEX和DROP权限可用；探针表已清理。
- Node数据库驱动首次健康检查因缺少可信CA证书返回`ssl_unavailable`，没有执行迁移。
- 从RDS实例SSL设置页提供的官方地址下载ApsaraDB CA证书，保存到Git忽略的`.local/certs/`，并通过`.env.local`配置本地证书路径。
- 使用官方CA后`db:health`通过：目标数据库匹配、MySQL主版本8、SSL启用、UTC会话生效、连接池上限5。
- 迁移前`db:status`确认迁移历史为空、12张预期业务表均不存在，没有同名冲突。
- 首版迁移成功应用版本1并创建12张采集阶段业务表；迁移后状态检查确认没有缺表。
- 第二次执行迁移时应用数量为0且没有待执行版本，幂等性验证通过。
