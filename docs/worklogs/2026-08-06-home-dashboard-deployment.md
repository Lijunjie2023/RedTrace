# 首页驾驶舱与数据管理线上部署记录

日期：2026-08-06

## 部署内容

- 将小红书采集探针与入库约束提交 `fed2ec2` 推送到 GitHub `main`。
- 将首页驾驶舱、品牌与品类筛选联动、负面问题证据联动和数据管理页面提交 `f9c5502` 推送到 GitHub `main`。
- ECS `/opt/readtrace/current` 通过快进方式更新到 `f9c5502`。
- 使用锁文件重新安装依赖，执行类型检查、完整测试和生产构建。
- 将 `apps/web/dist/` 同步到固定发布目录 `/var/www/readtrace/`，重启 `readtrace-api.service`。

## 验证结果

- 线上类型检查通过。
- 线上完整测试：101 项通过，0 项失败，0 项跳过。
- 前端生产构建通过，新资源为 `index-CkPq2SgT.js`。
- `readtrace-api.service` 状态为 `active`。
- API 会话接口返回 HTTP 200；数据管理接口在未登录状态返回预期的 HTTP 401。
- HTTPS 首页返回 HTTP 200。
- 数据库健康检查通过：MySQL 主版本为 8，数据库匹配，SSL 已启用，会话使用 UTC，连接池上限为 5。

## 已知事项

- ECS 仍使用 Node.js 20.20.0，`cookie@2.0.1` 声明要求 Node.js 22；本次测试、构建和运行验证均通过，但后续仍需安排 Node.js 22 升级验证。
- `npm ci` 报告两项高严重度依赖问题。本次没有自动执行修复，避免引入未经评估的依赖升级。
- 8443 当前使用自签名证书，严格 TLS 校验会失败。服务连通性已验证，但正式使用前应更换为受信任证书。
