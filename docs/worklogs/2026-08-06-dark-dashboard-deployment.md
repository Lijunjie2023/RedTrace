# 暗色舆情看板线上部署记录

## 部署内容

- 将暗色应用壳、舆情概览改版和 MySQL 概览聚合提交 `cbce844` 推送到 GitHub `main`。
- ECS 部署目录通过快进方式更新到该提交。
- 重新安装锁定依赖，完成类型检查、全量测试和生产构建。
- 将前端静态产物发布到 ReadTrace 专用 Nginx 目录，重启 API 并重载 Nginx。

## 验证结果

- 全量测试：91 项通过，0 项失败。
- 前后端类型检查与生产构建通过。
- 数据库健康检查通过：MySQL 8、SSL、UTC 会话和数据库名称均符合配置。
- HTTPS 首页返回 200。
- 管理员会话接口返回 200。
- 真实概览接口返回 200，并读取到帖子、评论、负面数量和健康状态。
- API 仅监听本机 3100 端口，Nginx 监听 8443 端口。

## 后续事项

- `npm audit` 报告 2 个高严重度依赖问题，需要单独评估升级影响后处理，不能直接执行自动修复。
- ECS 当前使用 Node.js 20；依赖树中的 `cookie` 声明需要 Node.js 22。当前测试、构建和运行验收均通过，但应安排 Node.js 22 升级验证。
