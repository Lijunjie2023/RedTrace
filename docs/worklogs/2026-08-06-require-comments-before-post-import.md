# 帖子入库增加评论门槛

- 日期：2026-08-06
- 产品规则：帖子至少取得一条评论后才允许写入业务数据库。

## 改动

- 将规则写入项目`AGENTS.md`，作为所有后续数据来源的统一入库标准。
- 在现有探针数据库导入层增加评论数量判断。
- 相关性、正文和评论三项条件必须同时满足，帖子才进入事务持久化批次。
- 没有评论的帖子仍可保留在Git忽略的本地采集产物中，便于排查采集覆盖，但不会写入业务数据库。

## 历史数据

数据库采集任务94已经写入的3篇卡萨帝帖子不自动删除。删除既有数据会影响任务、互动快照、品牌命中和原始快照等引用，需要用户明确授权并单独制定清理方案。
