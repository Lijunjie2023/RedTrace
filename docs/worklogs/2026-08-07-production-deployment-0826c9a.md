# 生产部署记录：0826c9a

日期：2026-08-07

## 部署目标

- 生产环境目标提交为 `0826c9a024f60c8e33b26832e049901d8cea39a5`（短提交号 `0826c9a`）。
- 部署前保留了代码与静态资源回滚点，原生产提交为 `3afa27bdbe3012ea26406287ae816da5d3d45bb9`。
- 本次发布没有把密码、Token、Cookie、数据库连接串或完整环境配置写入命令输出、文档和提交记录。

## 数据库备份与迁移

- 数据库迁移前确认阿里云 RDS 备份集 `3109532480` 已经完成。
- 备份开始时间为 2026-08-07 14:34:07，完成时间为 2026-08-07 14:34:29，保留至 2026-08-14。
- 在备份可用后执行迁移版本 4、5。
- 最终迁移状态显示版本 1 至 5 已经全部应用，`pendingVersions` 为空。
- 数据库结构检查结果为 18/18 张预期表均存在，没有缺失表。

## 构建与测试

- 类型检查通过。
- 普通 `npm test` 在 Node.js 20 环境中受到测试运行器加载 TypeScript 文件的回归问题影响，最终报出 `ERR_UNKNOWN_FILE_EXTENSION`。失败发生在运行器加载阶段，不是业务断言失败。
- 使用等价命令 `node --import tsx --test --test-concurrency=1 .test-dist/tests/*.test.js` 重新执行完整测试，131/131 项全部通过。
- Web 生产构建完成，并将生成的静态资源发布到生产静态目录。

## 发布与验收

- 生产仓库 HEAD 与目标提交 `0826c9a024f60c8e33b26832e049901d8cea39a5` 一致，工作区干净。
- `readtrace-api.service` 为 `active`，API 只监听 `127.0.0.1:3100`。
- 外网 HTTPS 首页、`/api/v1/session`、JavaScript 和 CSS 静态资源均返回 HTTP 200。
- `readtrace-analysis.timer` 保持 `disabled` 和 `inactive`，避免分析任务再次与 API 争抢有限内存。

## 服务器异常负载

- 部署期间服务器出现异常高负载，排查发现大量 `pm2 describe wechaty-bot` 和 `pm2 show wechaty-bot` 监控查询进程长期或并发运行。
- 这些进程不属于 ReadTrace。为避免影响同机外部业务，本次没有终止、重启或删除任何相关进程，也没有操作 wechaty-bot 主服务。
- 在不清理外部进程的情况下，服务器一分钟负载从 40.69 继续回落至 34.05；RedTrace API 在负载仍然偏高时保持可用。

## 后续事项

- 当前 ECS 的内存与交换空间压力仍需单独治理，并检查 wechaty-bot 监控脚本为什么持续生成 PM2 查询进程。
- 在完成服务器扩容、同机服务减负或正式交换空间规划前，继续保持自动分析定时任务关闭。
