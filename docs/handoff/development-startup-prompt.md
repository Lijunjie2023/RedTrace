# 下一开发任务启动提示

将下面内容作为新开发对话的第一条消息。发送账号、Cookie、数据库密码和模型密钥时，不要把真实值直接粘贴到对话正文，优先使用本地环境变量或双方确认的安全方式。

```text
我们要开始开发ReadTrace第一版。项目路径是E:\WorkSpace\ReadTrace。

先完整读取项目根目录AGENTS.md、tasks/prd-xiaohongshu-public-opinion-v1.md、docs/worklogs/2026-08-06-product-scope-discussion.md，以及AGENTS.md要求的全局规则。按照项目规范执行。

先确认Git仓库边界。当前检查发现Git根目录可能位于ReadTrace项目上级，并包含大量与本项目无关的改动。不要清理、暂存或提交这些改动。给出安全的仓库处理建议，获得我确认后再执行Git初始化、迁移或提交。

本次目标是先完成本地版本，暂时不部署云服务器。开发期间直接连接我提供的云数据库。系统使用单管理员账号，每小时更新一次小红书数据，使用DeepSeek V4完成自动分类和原因归纳。

不要立刻编写业务代码。先检查当前仓库状态，并给出分阶段开发方案、技术选型、目录结构、数据模型草案、外部依赖、主要风险和验证顺序。我确认方案后再开始实现。

第一阶段必须先验证真实数据路径：
1. 验证小红书账号登录状态如何安全提供和保持。
2. 验证按品牌词取得帖子。
3. 验证取得帖子正文、发布时间、原始链接、点赞数、收藏数和评论数。
4. 验证取得评论、回复关系和评论点赞数。
5. 验证后续任务能否更新已有帖子的互动数据。
6. 记录可取得字段、缺失字段、访问限制、账号风险和平台规则风险。

真实数据验证完成前，不要先做大批页面，也不要用模拟数据宣称采集能力已经完成。遇到平台限制时，把证据、影响范围和可选方案列出来让我决定。

技术边界：
- 真实账号、Cookie、数据库连接串、密码、Token和DeepSeek密钥不能进入代码、文档、日志、测试数据和Git提交。
- 云数据库连接信息和DeepSeek配置通过环境变量提供。
- 模型名称使用我提供的DeepSeek V4具体标识，模型调用需要固定字段输出、格式校验、超时、有限重试和失败记录。
- 品牌、品类、问题类型和原因主题要能配置，不能硬编码成无法扩充的逻辑。
- 帖子、评论、互动历史、模型原结果和人工修正结果需要分开保存。
- 定时采集需要去重和幂等，同一数据重复取得时更新已有记录。
- 所有分析结论必须能够回到原帖或原评论。
- 第一版只做PRD中的P0，不增加工单、提醒、多角色、多平台、自动周报、视频理解或云部署。

编码流程、测试职责和subagent委派必须遵守AGENTS.md及其引用的rules/sub_agent_dispatch.md。较大改动先给方案，等我确认后再执行。

第一条回复请提供：
1. 仓库现状。
2. 推荐技术方案及选择理由。
3. 分阶段实施计划。
4. 第一阶段数据验证清单。
5. 需要我通过安全方式提供的配置字段名，不要索要在对话中粘贴真实密钥。
6. 需要我确认的关键选择。
```
