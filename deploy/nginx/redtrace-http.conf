server {
    listen 80;
    listen [::]:80;
    server_name redtrace.xin;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type text/plain;
    }

    location / {
        return 301 https://redtrace.xin$request_uri;
    }
}
